apiVersion: v1    
kind: Pod     
metadata:
  name: webapp 
  namespace: dev 
  lables:
    app: weapp  
spec:
  containers:
  - name: webapp   
    image: mylandmarktech/maven-web-app   
    ports:
    - containerPort: 8080  